import React from 'react';

const Navbar: React.FC = () => {
  return (
    <nav className="w-full h-16 bg-sky-500 flex items-center px-6 shadow text-white font-semibold text-xl">
      Kolderkid
    </nav>
  );
};

export default Navbar;
